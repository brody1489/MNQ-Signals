# Force Python build plan so Railway doesn't get confused by monorepo
providers = ["python"]

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
# Unbuffered stdout so Railway deploy logs show every print
cmd = "python -u run_rth_loop.py"
