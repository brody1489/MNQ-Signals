# Discord — NEW bot for website only (create at discord.com/developers/applications)
DISCORD_BOT_TOKEN=your_website_bot_token_here

# Your Discord server (guild) ID
GUILD_ID=your_guild_id

# Channel ID where the daily recap is posted (bot will read last message here)
RECAP_CHANNEL_ID=your_recap_channel_id

# Your Discord user ID — contact form submissions are DMed here
# Enable Developer Mode in Discord (Settings → App Settings → Advanced), right-click your name → Copy User ID
CONTACT_DM_USER_ID=your_discord_user_id

# Optional: role name for "analyst" (used to match names and get PFPs). Default: analyst
ANALYST_ROLE_NAME=analyst

# Join Discord link (written to config.json for the site)
DISCORD_INVITE_URL=https://discord.gg/YOUR_INVITE_CODE

# Optional. Contact form only DMs you; no email is shown on the site. Omit if you don't need it.
# CONTACT_EMAIL=

# Port for the web server (site + API). Same as PORT on Droplet.
PORT=3000

# Where the static site files live (absolute path on server, e.g. /root/website/site)
SITE_DIR=

# Data directory for totals and generated JSON (default: ./data)
DATA_DIR=./data

# Cron: 8:30 PM daily. Format "minute hour * * *" (UTC). Example: 30 20 = 8:30 PM UTC. Adjust for your timezone.
CRON_DAILY=30 20 * * *
TZ=UTC
